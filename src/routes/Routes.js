import { BrowserRouter, Route, Switch } from 'react-router-dom';
const Routes = () => {
  return (
    <BrowserRouter>
      <Switch>
        <Route to='/'></Route>
      </Switch>
    </BrowserRouter>);
};

export default Routes;